## DOCS-58：发送后最新消息可见（2026-09-12）

实际故障会话的两轮 Run 均已完成，数据库和浏览器 DOM 中都有输入与回复；缺失的是发送后的滚动跟随。旧实现沿用“正在阅读历史”的状态，发送不会回到底部；已结束对话上翻时，返回按钮也要等内容变化才显示。

共享 `createChatPage` 现在在发送开始时恢复跟随（包括重试和程序调用 Controller），上翻立即显示“回到最新消息”。被动轮询与流式更新仍尊重后续上翻，不把读历史的用户持续拉回。没有改变请求、重试幂等、存储或模型执行。

验证：类型检查、构建通过；使用 CUA 在隔离合成 Engine/HTTP 宿主中复现旧问题，再验证新实现。762px 页面与 418px 浮窗中，从历史处发送后的底部距离为 0 / -1px（浏览器取整），最新一轮可见；已完成对话上翻立即显示入口；键盘发送、运行中上翻经过多次轮询后位置不变（207.5px）、返回最新与取消通过，浏览器错误 0。补充 `scripts/verify-chat-ui.ts` 自动断言，本轮以实际浏览器操作核验相同路径，未重跑整份浏览器回归或无关数据库矩阵。窗口尺寸为容器验证，不作真机声明。

标准 4320 宿主在活动 Run 为 0 时更新并重启，静态资源与新构建哈希一致；原会话消息仍在。本轮没有新增真实模型请求。产品视觉保持现有设计，只修复交互状态。

## DOCS-51 至 DOCS-57：文档站 SDK 能力覆盖（2026-09-12 最新）

本轮将此前未装配的能力接入标准文档助手，并提供可重复执行的配置实验。下方“6 个工具”“未接入 Skill/Memory”“等待输入只展示”等属于此前阶段的记录，以本节为准。完整配置、API 与范围见 [能力接入总结](../../docs/docs-capability-coverage.md)。

| 行为卡 | 可观察契约 | 实际证据 |
| --- | --- | --- |
| DOCS-51 助手能力目录 | 6 个 Binding + HTTP/MCP 两个工具、inline/loader 两个 Skill、manual 知识库；配置仅能收窄宿主目录 | 实际 HTTP/MCP 初始化与调用、Skill 版本/哈希和知识检索集成；4320 当前目录 8/2/1，与模型实际调用一致 |
| DOCS-52 长期记忆 | 同一可信访客跨会话读写，写前展示真实提案并确认；读写独立开关；内容加密、CAS、幂等、到期清理和删除 | 身份隔离、禁读、批量提案预览、旧版本冲突、删除重建 ABA、过期密文清理；独立 PostgreSQL 审批中重启后继续同一 pending，新会话读取；实际启动器重启后保留记忆 |
| DOCS-53 原生交互 | question / structured_input / permission 从 IM 回答，继续同一 Run；轮询保留表单草稿 | Engine/HTTP Schema 拒绝、同回复幂等、跨身份/Origin 拒绝；浏览器追问、表单和记忆批准/删除链路，移动表单字段及提交可达 |
| DOCS-54 配置实验 | 13 类、21 条组合路径；结构化输出/引用修复、重试/Fallback、工具修复、写入核验、预算、压缩、配置冻结、事件/可观测性与生命周期 | 21 项实验断言真实状态、事件、模型/工具次数；失败分支校验具体错误；游标订阅和 SDK exporter 实际执行；浏览器开关正反路径与报告幂等 |
| DOCS-55 会话调试 | 仅本人会话，真实配置快照和最多 1000 条事件，归档/恢复/删除；活动 Run 拒绝生命周期变更 | 真实 Engine.inspectSession / listEvents 与权限集成；浏览器选择、归档、恢复、删除及焦点返回；截断明确标识 |
| DOCS-56 失权与连续操作 | 401 清除所有旧身份数据、使在途响应失效、暂停控件并提供重连；403 清理被拒记录并更新有效列表 | 浏览器延迟 200 响应晚于 401 时不恢复旧数据，403 后有效列表刷新；记忆/会话取消、删除与归档后焦点落到有效控件，浏览器错误 0 |
| DOCS-57 交付与真实入口 | 通用 resolveInput / pending 组件可独立消费；文档与实际启动器一致 | 独立包安装、声明、Node SSR 导入、浏览器 bundle 和隔离检查通过；4320 当前资源哈希匹配，PostgreSQL 目录和重启回读通过 |

验证命令与结果：

- `pnpm exec vitest run --exclude '.local/**'`：38 文件 / 192 项通过，0 跳过，含真实 PostgreSQL 和故障恢复。此前默认发现规则也运行了其他并行任务在 `.local` 内的验证副本；不把其重复用例计作新增覆盖。
- `pnpm typecheck`、`pnpm build`；`pnpm exec tsx examples/docs-site/verify.ts`：25 篇 / 33 组；`node scripts/pack-chat.mjs` 与 `node scripts/verify-chat-packages.mjs`：安装/声明/Node/浏览器消费通过。
- `pnpm verify:docs-capabilities`：真实 Engine/HTTP/Chromium、合成模型，共 6 次模型调用，12 张桌面/手机/明暗及失权状态截图，浏览器错误 0。13 类实验的全部组合由集成测试覆盖，浏览器只操作代表路径。
- 已授权真实模型 4 个 Run 完成：确认偏好写入、新 Session 读取，loader Skill + KnowledgeBase/HTTP/MCP，以及 inline Skill + 文档查询；真实数据只使用本次合成偏好。实际启动器重启后该记忆仍可读取，最后回读和资源检查模型调用 0。

实际重启暴露清理任务在 Engine 锁取得前启动的 `STORE_LOCK_LOST`，已把初始化移至 Engine 成功创建之后，重新启动和持久化检查通过。关闭流程停止清理定时器、主动关闭 HTTP 长连接再释放 Engine。没有放宽 Store fencing 契约。

配置实验使用独立 MemoryStore + Testing 脚本模型，不代表真实供应商或 PostgreSQL 崩溃恢复已由实验验证。标准启动器实际使用独立 PostgreSQL；上述恢复和供应商验证分开执行。匿名记忆仅对应当前浏览器签名身份，最近 5 条偏好读取不是语义向量检索，也不是所有旧会话全文回放。已有配置偏好保留限制，需恢复助手默认并保存或逐项启用新增能力。

独立终审对 F1（失权清理）、F2（焦点返回）、F3（产品/设计事实同步）均判定 resolved，disposition: ship，仅覆盖这三项修复；审查者查看十二张有效截图并核对源码、文档和交接证据，没有重新运行浏览器或 Provider。一次检测器的 17px 小标题 advisory 已纳入宿主设计记录；范围外既有 Markdown 边线和回复动画没有因此重做。未新增 ASR、产品安装发布、真实 replay、分布式调度或监控产品。原有源码白名单、身份与宿主安全上限保持；截图、Cookie 和私有验证输出均在忽略目录。

## DOCS-47 至 DOCS-50：IM 会话配置（2026-09-12）

| 行为卡 | 可观察契约 | 实际证据 |
| --- | --- | --- |
| DOCS-47 入口与界面 | IM 配置打开独立网页，创建空会话不发消息；工具/Skill 搜索、知识/记忆读写、回答/上下文四类；未接入能力明确说明 | 配置专项真实 Engine/HTTP/Chromium，草稿不丢、四类操作、键盘/搜索、明暗主题/减少动态、9 张合成状态截图；实际 AI 和传统浮窗入口通过 |
| DOCS-48 生效与权限 | 宿主显式开放，前端只能收窄；Session 后续轮次生效，活动 Run 冻结；静态工具禁用、记忆读取完全禁用 | 4 项新 HTTP 集成测试，实际 ModelRequest 与记忆 Binding 次数断言；未知 ID/字段、来源、其他身份、宿主禁用、归档拒绝；旧策略/回执回归保留 |
| DOCS-49 保存与恢复 | 版本冲突不覆盖，网络错误保留草稿，丢失响应先核对，撤权清除旧目录，成功读取才恢复；宿主迁移保留限制 | 浏览器 CAS/丢失响应与 401/403/host disabled 保存、再次读取和恢复；Engine 目录迁移/新增能力不自动开启；真实 PostgreSQL 保存与宿主重启回读通过 |
| DOCS-50 交付与文档 | 通用页面/类型/传输可独立消费，实际文档站可操作，SDK 语义变化记录 | 独立包安装、声明、Node 与浏览器构建通过；公开场景文档、README、前端 SDK、技术设计和总结同步；独立终审 F1/F2 resolved、ship，仅两项修复范围 |

`pnpm check`：33 文件 / 162 项、0 跳过，包含真实 PostgreSQL 和故障恢复；25 篇 / 33 组文档、20 组聊天回归通过。最后 UI 修正后的类型检查、构建、配置专项与独立包消费均通过。新测试曾发现静态 deny 仍向模型声明、strategy:none 不足以完整禁读，已按明确契约修复并验证；旧回执 deny 测试改为断言前置拒绝、没有业务策略/写入/受限回执泄漏，require-approval 检查未放宽。

4320 本地实际配置：6 个只读工具、Skill 0、知识库 0、跨会话记忆存储 0；网页保存到真实数据库、刷新恢复、服务重启后保留禁用，再恢复默认；模型调用 0、浏览器错误 0。第一次重启遇到旧 HTTP 连接持有的 Engine 锁，确认活动 Run 为 0 后结束旧进程，后续启动及验证通过。未测试新真实供应商请求，因为本轮保存不调用模型；记忆/Skill 启停由合成宿主的真实 Engine 执行验证。

独立审查初次要求修复失权后仍可编辑及读取时误报保存；修正后两项均 resolved，disposition ship，九图有效。检测器仅保留范围外中性 Markdown 引用边线、既有回复高度动画提示，标题 26/23px 纳入设计记录；未扩大为完整 WorkBuddy 或原生手机系统认证。截图、Cookie 和本地验证数据继续在忽略目录。API 与接入见 [IM 会话配置](../../docs/chat-settings.md)。

## DOCS-43 至 DOCS-46：回复与公开思考展示（2026-09-12 最新）

| 行为卡 | 交付行为 | 实际证据 |
| --- | --- | --- |
| DOCS-43 展示权限 | model expose、host thinkingDisplay、保留策略共同控制公开正文；summary / content 分开；原生签名不显示 | chat-process 与双协议集成：默认关闭、显式启用、summary 不升级、撤权缓存重读、失败尝试撤回、签名隔离 |
| DOCS-44 内容更新 | 思考、阶段说明、工具与答案分开；短缓冲、稳定 Markdown / 代码复制焦点、历史直显、减少动态效果 | 新 verify:reply-display 真实 Engine/HTTP/Chromium 验证多帧与字素、复制/焦点、滚动、刷新、销毁；五张合成状态截图 |
| DOCS-45 层次与回归 | 当前阶段、思考与工具突出；已结束基础状态进执行详情；完成过程默认收起 | 独立复核 disposition ship，范围为五张截图与所抽查 UI 代码；文档 25 篇/33 组、聊天 20 组、图片、输入与过程专项通过 |
| DOCS-46 真实入口 | 本地文档宿主显式开启 content + session；空闲旧会话迁移，新轮可展示，旧未保留内容不可恢复 | 已授权默认视觉 Profile + PostgreSQL：3 次模型请求、3 次资料工具调用、3 段公开思考（113/243/35 字符）、10886 Token、刷新后分区可读、浏览器错误 0 |

本地宿主重启后再次检查：浏览器包哈希匹配最新构建，思考文档已上屏，授权历史的三段思考仍可回读。

完整回归 `pnpm check`：32 文件 / 157 项，包含 PostgreSQL 和故障恢复，0 跳过。独立包安装、声明、Node/浏览器消费和三条导出示例的安装/类型检查/实际场景通过。旧聊天脚本原本等待重复“已完成”状态可见，本次已改为等待真实完成状态并验证答案可见，失败/取消状态仍须可见。

真实验证边界：第一次选择 deepseek-thinking Profile 返回 MODEL_PROVIDER_ERROR，未验证该 Profile 可用；随后默认 primary（deepseek-vision）成功。两种标准协议的公开思考/原生隔离均由本地 HTTP 集成验证，不能据此宣称每个真实供应商均已验证。不展示模型未返回的推理，不读取受保护原生块。仍是有界轮询而非 SSE，每块公开思考最多回读 8000 字符。

实现与配置见[回复展示改进](../../docs/reply-display-alignment.md)。下方验收记录保留各次交付时的范围，新的公开内容开关扩展了之前仅状态的默认行为。

## DOCS-39 至 DOCS-42：输入编辑与模型选择（2026-09-12 最新）

| 行为卡 | 可观察契约 | 实际证据 |
| --- | --- | --- |
| DOCS-39 编辑 | 原生撤销/重做和选区、自动增高/展开/Esc、两种发送模式、中文 IME、空输入 ↑ 找回上一条文字 | `scripts/verify-composer.ts` 经真实 Engine/HTTP/Chromium 验证；100ms 后独立发送与 80ms 候选确认保护分别断言 |
| DOCS-40 选择 | 服务端模型/Skill 目录、下一轮选择、受理冻结/重试、刷新和撤销选项 | 新 core/HTTP 契约；模型请求实际命中所选 model 和 Skill 指令；目录不含 Key/地址/指令；图片草稿不因纯文本模型丢失 |
| DOCS-41 能力与移动端 | 无 ASR 时麦克风禁用；可键盘读取原因；正常名称在 320/390px 完整可读 | 四张明暗/桌面/手机实际截图、尺寸断言；F1 截断修复经独立复核 resolved，disposition ship，仅该项范围 |
| DOCS-42 旧会话和实际入口 | 授权读取空闲旧会话更新目录，保留历史、不调用模型；当前 Run 配置冻结 | 文档验证追加迁移断言；4320 真实 DeepSeek 普通模型切换、1 次请求 / 1527 Token、刷新恢复、浏览器错误 0 |

`pnpm check` 32 文件 / 153 项（真实 PostgreSQL、0 跳过）；25 篇 / 33 组文档、20 组聊天、输入/图片/过程专项通过；独立安装包声明、Node 导入、浏览器构建与隔离通过。原生撤销脚本的粒度假设和旧 IME 测试时序已按浏览器实际语义修正后通过。实现和公开 API 见[输入编辑与模型选择](../../docs/composer-alignment.md)。

边界：语音服务、任意文件/工作区引用未接入；通用 SDK 默认 mod-enter，本站显式 enter；选择/快捷键偏好不跨刷新持久化。原生历史不兼容的模型切换明确报错，使用原模型或新会话，不丢弃历史。WorkBuddy 仅只读源码行为参考，未作线上逐像素或全功能验证；iOS 真机软键盘尚未验证。

## DOCS-35 至 DOCS-38：聊天处理过程（2026-09-12 最新）

架构支持，直接补齐公开过程数据与界面，不重做执行引擎。完整分析及 API 变化见[聊天过程架构与实现](../../docs/chat-process-architecture.md)。

| 行为卡 | 可观察契约 | 实际证据 |
| --- | --- | --- |
| DOCS-35 实时过程 | 真实模型阶段、Skill、工具、知识/记忆、重试、上下文整理与等待按 sequence 展示；最终答案独立 | 真实 Engine 集成和 HTTP/Chromium 专项；思考正文、提示词、私有参数结果均未进入公开 DTO |
| DOCS-36 回读与边界 | 刷新可恢复；缓存仍重新授权；超出保留范围明确提示；跨轮重复 callId 不混淆结果 | 4 项 chat-process 契约覆盖安全摘要、授权撤销、重试草稿、取消、历史恢复、重复标识和事件过期 |
| DOCS-37 阅读连续性 | 执行时展开、完成可收起、主动查看状态保留；工具结果更新不移动内部焦点和滚动位置 | 长输入详情聚焦/150px 滚动，完成后同一 DOM/焦点/滚动断言；代码复制焦点、390px/1440px 页面无溢出及卸载通过 |
| DOCS-38 主题和实际入口 | 自定义浅色强调色仍可读；只展示已知耗时/用量/估算费用 | #ffcc00 下状态对比度 ≥4.5、焦点 ≥3；4320 真实 DeepSeek + PostgreSQL 两次模型请求/一次工具查询、思考状态与刷新恢复通过 |

验证：pnpm check 32 文件/150 项，0 跳过；25 篇/32 组文档、20 组聊天和图片浏览器回归通过。新 pnpm verify:chat-process 通过，页面错误为 0。真实 smoke 首次浏览器断言失败，对应数据库 Run 已完成；调整脚本显式等待最新快照后复验通过，见实施计划。没有把合成截图内的思考或 Token 当作真实供应商证据。

独立终审首轮提出自定义主题对比度、更新时详情焦点/滚动两项修复；两项均经同一审查者判定 resolved，disposition: ship（所列修复范围）。八张有效截图位于忽略目录 .impeccable/review/process-*.png；新增浅色强调色键盘焦点和长详情完成状态证据。沿用原有视觉系统。

边界：不显示私有思考正文；模型未报告的阶段、未配置的 Memory/Skill 和无费率的费用不会虚构。等待问题可见，但回答、审批及核验继续由宿主自己的授权接口处理。文档助手未新增记忆或插件安装；过程使用有界快照轮询，非 SSE、非无限日志。

## DOCS-29 至 DOCS-34：场景接入与图片（2026-09-12 最新）

本轮用户授权修改 API。以下是当前实现证据；后文“纯文本”“20 篇”“两个入口”等表述属于历史批次，现由本轮扩展。

| 行为卡 | 可观察契约 | 实际证据 |
| --- | --- | --- |
| DOCS-29 场景接入 | 25 篇文章按环境、会话、工具、Skill、知识、记忆、图片和 Debug 等任务组织；三条入口都能运行 | 32 组文档验证、受检源文件与渲染代码逐字比较；导出三个独立目录安装/类型检查，后端重启及五个场景、全栈登录/刷新、独立前端跨域发送通过 |
| DOCS-30 产品形式 | 源码、本地 SDK 可用；npm/CLI/压缩包/安装包保留待确定入口，无假下载 | deliveryForms 契约检查；320px/1440px 三个等价接入按钮与交付文案截图复核 |
| DOCS-31 API 易用性 | 类型化作者输入、defineBoundTool、字段路径错误、RunInput.skill；精确默认值和授权可查 | scenario-api 契约与独立工具/Skill 脚本运行通过；旧文本、模型选择 Skill 与手工 Binding 回归通过 |
| DOCS-32 图文会话 | 选择/粘贴/拖拽、上传错误重试、预览移除、纯图片、多图和刷新历史 | 独立图片浏览器脚本验证实际模型输入字节、图片历史、粘贴/拖拽及销毁；真实视觉三阶段验证见下 |
| DOCS-33 图片可靠性 | 加密、同主体授权、冻结摘要、失效/撤权、上传中断清理、历史工具图片、重启恢复 | 8 项图片集成检查；真实 PostgreSQL 受理后崩溃恢复；模型重试前撤权阻止再次派发；外部存储损坏/版本变化/到期清理 |
| DOCS-34 阅读与回答 | 搜索显示命中正文和章节锚点，代码带文件名/高亮/复制；聊天安全 Markdown 和代码复制 | 章节 ID/代码源文件/高亮/搜索点击检查，20 组通用聊天回归和图片浏览器恶意 HTML/URL/复制检查通过 |

`pnpm check`：31 个文件、146 项通过，含真实 PostgreSQL，0 跳过。文档 25 篇/32 组和聊天 20 组浏览器检查通过；图片专项覆盖新增交互，浏览器错误为 0。最终导出目录重新安装与运行通过。4320 实际服务已重启，三个入口、视觉能力配置和页面挂载通过，最后这次本地入口检查未调用模型。

真实视觉：已授权 DeepSeek 视觉 Profile，两个合成图片的随机编号/顺序、重启后颜色追问、工具返回历史图像三阶段通过；普通事件/记录不含图片原文。OpenAI 和 Anthropic 兼容视觉块通过实际本地 HTTP 验证，Anthropic 视觉没有真实供应商 smoke，不扩大该证据。

独立 Impeccable 终审 disposition: ship。审查覆盖五张指定桌面/手机截图与实现抽查；故障、剪贴板和焦点由浏览器检查提供行为证据，不把审查截图当作交互实测。完整终审与一次静态 detector 在忽略目录 `.impeccable/review/scenario-*`；主要截图为 desktop.png、intro-320.png、desktop-article.png、chat-images-desktop.png、chat-images-mobile.png。设计文档与 sidecar 同步此次实际变化，既有视觉身份保留。

限制：正式交付方式未定、未发布/部署；图片草稿不跨页面保存；过期或撤销引用会阻止相关历史读取和发送；maxImageInputTokens 需按实际模型设置。普通文本调用保持兼容。完整 API 迁移与验证说明见[优化总结](../../docs/usage-optimization-summary.md)。

## DOCS-24 至 DOCS-28：双模式体验优化（历史）

- DOCS-24：通用 SDK 将标题和会话/工具操作合并，左栏提供新对话，收起时页头保留图标入口；输入与发送操作共用色面，接近上限再强调字数。
- DOCS-25：文档站宽屏使用 440px 右侧助手栏，正文让出空间；1000px 以下原生全屏，列表仍可展开，工具与当前会话仍可访问。
- DOCS-26：AI / 传统模式保留文章、滚动位置和草稿；“继续阅读”可移除。阅读位置是导航记录，不会自动作为模型上下文发送；询问本文只填入可编辑问题。
- DOCS-27：已完成回答各自显示白名单核对的参考链接，源码包含提交与行号；SDK 拒绝脚本协议、凭据 URL 和 HTML 标签注入。
- DOCS-28：目录按当前分类展开；前端示例支持 JavaScript / React / Vue 键盘标签和深链接，无 JS 可顺序阅读、打印显示全部。

本轮类型检查和构建通过；32 组文档站确定性检查、20 组通用 SDK 浏览器检查及 7 项 chat-core 契约测试通过，浏览器错误 0。新增检查覆盖停靠不遮挡、阅读位置/草稿恢复、逐条引用、框架键盘操作/深链接与引用 URL/文本安全。

桌面/窄屏和明暗主题在一轮截图批次核对；修正输入框重复焦点轮廓后，以当前 4320 服务确认 1280×720 和 390×844。前者对话记录可视高度 357px，正文与 440px 助手不重叠；后者全屏展示并保留发送及会话/工具入口。证据保存在忽略目录 .impeccable/review/refined-live-*.png。当前服务保留真实模型与独立 PostgreSQL，本轮 UI 验证未发送真实模型请求；移动验证是 Chromium 视口，不是真机软键盘认证。

静态扫描一次：21 条 advisory、0 warning、0 critical，主要是既有响应式字号/圆角与设计文档色阶的差异；不把它们计为已证实的 UI 错误。设计与使用文档同步本次改动。Markdown 富文本仍未实现，不包含在本轮交付声明中。

---

# 文档站验收记录

本功能为独立文档宿主，参考现有聊天 SDK 公开契约。不是 WorkBuddy 行为移植。日期：2026-09-11。最新追加范围见下方“传统模式产品介绍”；早期记录中的“未更改核心包”仅指当时交付。

## 传统模式产品介绍（最新追加范围）

DOCS-23：传统模式首篇为“产品介绍”，按一句话用途、核心特点、两列产品表、体验入口组织；前端 SDK 与后端 SDK 各有真实接入链接。导航、搜索索引与 AI 模式返回文档入口同步。删去原介绍页的重复入口和前置调用链路展示，具体配置保留在后续教程。

`pnpm typecheck`、构建及 28 组 `verify:docs` 通过。现有验证补充两个产品入口的点击／返回路径、320px 和 768px 产品表自身无横向溢出。已查看 1440px 浅／深色、390px、320px、768px 五张完整截图；两列关系与操作文字均可阅读。静态检测只有既有样式与设计规范不一致的 advisory，本次没有借此改动无关视觉值。此次为本线程的文案与布局复核，不冒充新的独立设计审查；没有重复调用真实模型。

## 对话侧栏与信息面板（最新追加范围）

| 行为卡 | 可观察契约 | 实际证据 |
| --- | --- | --- |
| DOCS-19 左侧对话列表 | AI 页面、传统浮窗复用 SDK 左栏；显示当前会话，点击可切换；窄容器通过按钮展开 | Chromium 两会话切换、当前标记、三轮历史恢复、模式间草稿保留；手机选择后关闭侧栏并返回焦点 |
| DOCS-20 当前会话 | 顶部入口显示 ID、助手、状态、创建时间、轮数与配置版本；查看时切换会话同步更新 | 面板打开后从左侧选择旧会话，内容切换到该会话 ID；开关面板保留输入 |
| DOCS-21 工具清单 | 从保存的会话配置投影公开名称、说明、读写与权限；区分当前配置和活动运行快照 | 两项真实 HTTP / MemoryStore 集成测试验证公开字段、私有说明/Schema/执行器隔离、跨用户拒绝及活动运行版本不漂移 |
| DOCS-22 窄屏与键盘 | 工具面板可滚动；Tab 可进入内容，Escape 只关闭内层面板再返回入口 | 390px 视口从关闭按钮 Tab 进入 region、End 到第六个工具、无横向溢出；Esc 后外层 IM 仍打开并恢复工具按钮焦点 |

以下为 DOCS-19 至 DOCS-22 当时记录（窗口尺寸已由 DOCS-25 更新）：`pnpm typecheck`、构建、2 文件 / 14 项后端与控制器测试、28 组文档站验证、19 组通用聊天 SDK 浏览器验证通过。文档浮窗为 880 × 720，主题更新保留尺寸；SDK 默认浮窗仍为 420 × 680。容器达到 760px 才常驻左栏，移动证据为 Chromium 视口，不代表真机软键盘验收。

工具数据是 chat-core 协议 v1 的可选扩展；未提供清单与空清单分别展示。服务端只发布显式 toolDisplay 文案和配置的工具摘要，不返回原始工具说明、执行器、参数 Schema 或凭据。已有身份隔离和 Engine 执行契约不变；本次没有重跑无关的崩溃恢复矩阵。

最新 `pnpm smoke:docs` 的四轮真实运行均完成，但第三轮回答没有明确给出 createSession，内容断言失败，不能计作完整通过；这是回答准确性的实际限制。下方较早四轮通过记录仅代表当次结果。本次界面的真实本地服务验证 `pnpm exec tsx examples/docs-site/smoke-panels.ts` 已通过：创建一个独立匿名空会话，核对 PostgreSQL 保存的时间／版本／六个工具、信息面板、刷新恢复与传统浮窗，共六项检查，不调用模型。

独立界面审查要求补齐详情滚动区的键盘入口，现已增加具名 region 与 Tab 焦点，End 可到最后一个工具，Escape 返回入口；复核将该项评为 resolved，disposition: ship，仅覆盖该修复。九张桌面／移动截图均有效；未把一次静态检查作为完整无障碍认证。

## 双模式与公开源码工具（最新追加范围）

| 行为卡               | 契约                                                                   | 当前证据                                                                                                                    |
| -------------------- | ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| DOCS-13 默认 AI      | `/` 进入 `/ai/`，使用 SDK `mountChatPage`；传统文档深链接仍有效        | Chromium 默认路由、SDK presentation 属性、模式切换通过                                                                      |
| DOCS-14 连续会话     | 两模式共享历史，同标签页保留草稿；旧会话下一次发送升级配置             | 两次来回切换、同 ID 三轮历史、草稿断言；将会话模拟为旧版后成功调用新增 API 工具                                             |
| DOCS-15 六个只读工具 | 文档搜索／原文读取、公开导出声明、示例、源码搜索／读取                 | 两条三工具查询链通过真实 Engine Binding 执行及输出 Schema 校验；文档按行分页逐字还原                                        |
| DOCS-16 源码版本边界 | 启动时 Git HEAD 的允许目录；不能读取密钥、符号链接、未跟踪或未提交文件 | 一次性 Git 仓库夹具验证；越界路径、私有文件、未知版本 HTTP 404；源码页路径与行号可见                                        |
| DOCS-17 双模式布局   | 浅深主题，320/390/768/1024/1440px 无页面或聊天区横向溢出               | Chromium 五宽度断言；欢迎标题从顶部开始，第三个推荐问题完整可见；未声称真机测试                                             |
| DOCS-18 真实查询     | 实际配置模型查询文档、API 声明与函数实现，来源可核对                   | 四轮真实 Provider + 独立 PostgreSQL：前端接入、普通聊天、后端续聊、createSessionMemory API／code.read；刷新恢复四轮历史通过 |

`pnpm typecheck`、`pnpm verify:docs`（26 组）、`pnpm verify:chat-ui`（19 组，浏览器异常 0）、`pnpm smoke:docs` 已通过。六工具确定性验证分两条查询链；不保证每轮都能用满所有工具。SDK 在供应商未报告 Usage 时保留保守预算，连续大检索可提前触发预算限制。文档助手每轮预算从 32000 调整为 64000，查询仍受步数、调用次数、返回字节与 90 秒时限约束。

真实查询记录位于忽略目录 `.impeccable/review/provider-smoke.json`。模型有时会缩写来源或不遵循简短字数；页面只将索引内有效路径变成可点击资料，不把模型文字当 HTML，也不把链接存在当成语义正确的证明。

本次修复聊天 SDK 的三个相关布局行为：空对话初始化与容器缩放从欢迎标题开始；长推荐问题换行；移除欢迎区与记录区重复的垂直留白。没有更改 Engine 执行语义、协议或数据库模型，也没有重复运行无关的 PostgreSQL 崩溃矩阵。

独立 UI 复核首轮结论为 fix（欢迎区密度、设计文档同步）。最终 disposition: ship；该结论只覆盖两项已列问题的修复确认：桌面浅深主题及 320px 截图的三个推荐问题完整可见；PRODUCT.md、DESIGN.md 与 sidecar 已合并双模式和共享状态事实。保留既有设计系统与 schema，没有新增栅格素材。检测器本轮运行一次，字号／内嵌圆角为 advisory，已由设计记录说明实际用法。改动文件与本地真实凭据精确匹配数为 0。

| 行为卡             | 契约                                                               | 验证方式                                    | 当前证据                       |
| ------------------ | ------------------------------------------------------------------ | ------------------------------------------- | ------------------------------ |
| DOCS-01 初次阅读   | 20 篇接入文档；前端 / 后端两条路线；深链接有服务端 HTML            | 渲染/链接断言、桌面和手机浏览器             | 已通过                         |
| DOCS-02 导航与搜索 | 中文关键词搜索、键盘选择、文章切换保留 IM 会话、代码复制           | Chromium 操作，空搜索/无结果查询            | 已通过                         |
| DOCS-03 默认 IM    | 使用现有 SDK 图标，询问本文填写草稿且由用户发送                    | 确定性模型、真实 HTTP、真实浏览器           | 已通过                         |
| DOCS-04 文档优先   | 项目问题先检索公开文档，普通聊天可以不检索，来源仅允许已知文章路径 | 真正执行 docs.search Binding + 合成模型链路 | 已通过；真实 Provider 另记     |
| DOCS-05 访客隔离   | 签名 Cookie，跨访客不能读取会话，伪造 Cookie / 外来 Origin 拒绝    | 真实 HTTP 与两个独立浏览器上下文            | 已通过                         |
| DOCS-06 路径与内容 | 私有文件不可访问、未知文档 404，原始 HTML / 危险链接不执行         | 精确路由、渲染恶意输入测试                  | 已通过                         |
| DOCS-07 响应式     | 五种宽度无页面横向溢出；手机菜单和全屏 IM 可打开/退出；主题可恢复  | 320 / 390 / 768 / 1024 / 1440px Chromium    | 已通过；未声称真实手机设备验证 |
| DOCS-08 仅阅读     | 无模型/数据库也可阅读、搜索；助手明确展示未连接                    | 独立只读宿主浏览器测试                      | 已通过                         |

## 本次方向修正与验证

用户明确要求文档说明“如何接入自己的项目、接入后如何使用”。首页提供前端 SDK 与后端 SDK 两个入口；安装、调用、配置、身份、会话、工具、知识库、事件和 Debug 按接入任务组织。本地 Playground、Docker 和示例凭据加载器降为附录。

| 行为卡               | 契约                                                             | 当前证据                                                                                                        |
| -------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| DOCS-09 两条接入路线 | 首页可直接进入前端 / 后端教程；说明前端仍需后端，后端可独立使用  | 两个首页入口可见断言；桌面、手机实际页面检查通过                                                                |
| DOCS-10 可复制代码   | 六份主要示例直接从受类型检查的源文件加载，显示内容与文件完全一致 | TypeScript 检查、Marked 代码块逐字比对、完整目录断言通过                                                        |
| DOCS-11 后端使用     | 配置解析、业务 Binding、createSession / run / loadSession 可用   | 确定性库存工具执行与续聊通过；原样执行 snippets/run.ts 的真实数据库 / 模型两轮对话通过                          |
| DOCS-12 接入问答     | 项目问题依据新手册回答，普通聊天不强制检索                       | 真实模型回答 mountChatWidget 与后端依赖；createSession / run / loadSession 与前端非必需；普通聊天和刷新恢复通过 |

本次 pnpm typecheck、构建与确定性浏览器 / HTTP 验证通过。六份示例中，业务登录与库存查询由接入方实现；文档明确标注这些宿主函数，不把它们写成 SDK 导出或默认授权。

真实文档助手使用独立的 agent_engine_docs 数据库。直接后端示例验证在一次性专用数据库中运行，结束后只删除该验证创建的数据库。本机模型走已授权开发代理，第一次直调返回 ACCESS_DENIED；补充只对模型 origin 生效的显式可选网络配置后，原样两轮调用通过。没有更改用户凭据文件或扩大默认网络权限。

真实问答记录分别保存在被忽略的 .impeccable/review/provider-smoke.json 和 backend-snippet-smoke.json。没有把供应商载荷放入 Git。此次文档范围 44 个可纳入 Git 的文件与真实凭据匹配数为 0。

## 首轮站点验证（历史记录）

本机默认真实 Provider + 独立 PostgreSQL 数据库已通过三轮连续问答：密钥配置问题实际执行 docs.search；普通聊天未调用检索；分布式调度与公共 npm 发布状态的回答符合当前项目资料。页面刷新后，三轮历史仍可恢复。默认回答使用适合当前 IM 的纯文本。

初次真实测试暴露连续检索片段过长导致 CONTEXT_BUDGET_EXCEEDED，已将每次最多三篇片段限制为 1600 / 900 / 900 UTF-8 字节，并启用既有历史工具结果裁剪和有界压缩配置。修正后的连续问答通过；新增字节上限及 Unicode 截断断言。没有提高模型上下文窗口来掩盖问题。

复现命令：`pnpm smoke:docs`，详细输出仅保存在被忽略的 `.impeccable/review/provider-smoke.json`。这不代表所有供应商、长会话或真实手机系统已经验证。

## 首轮设计验证（历史记录）

代码优先实现，继承现有工作台主题。桌面、手机、深色、入门文章与手机 IM 五份截图已查看；检测器返回 `[]`。首轮发现移动端悬浮提示文案遮挡正文，已移除手机上的提示文案，保留带可访问名称的图标。

本侧对话禁止子代理，终审和设计记录采用 Impeccable 的本线程降级流程，不冒充独立代理评审。第二轮五视图截图与最终真实问答截图已检查；结论为 ship，范围为这些视图与已执行的交互。移动端提示遮挡及默认回复 Markdown 装饰问题已解决；设计系统已记录在 DESIGN.md 和 .impeccable/design.json。

`pnpm typecheck`、六包构建、文档浏览器/HTTP 验证、真实 Provider smoke 和冻结锁文件检查通过；Git 可纳入的 161 个文件与本地真实凭据匹配数为 0。测试未运行生产外部动作，没有部署、提交或推送代码。

## 本次设计复核

本侧对话禁止子代理，采用本线程降级复核，不作为独立设计评审。沿用既有视觉系统，本次没有更换配色、字体或布局世界。

disposition: ship

### persistence

PRODUCT.md、DESIGN.md、设计 sidecar 和页面方向注释已改为前后端接入主线。

### fidelity

| 元素                             | 结论                                              |
| -------------------------------- | ------------------------------------------------- |
| 首页两条 SDK 接入入口            | match；桌面与手机首屏均可见                       |
| 接入、配置、使用、扩展的阅读顺序 | match；本地示例位于附录                           |
| TYPE                             | match；沿用中文系统阅读字体及等宽代码             |
| MATERIAL                         | match；文档采用分隔线和阅读色面，没有新增图片材质 |
| GROUND                           | match；沿用白底、深蓝文字及既有深色模式           |
| 询问本文与持久 IM 入口           | match；继续使用现有 SDK                           |

### ceiling

本次为信息架构与使用内容修正，视觉系统保持既有手册语言；没有新视觉概念或效果需求。

### material_fixes

首轮发现代码拼接使用字面换行，导致导入行和后续目录异常；已修正。第二轮截图中 TypeScript 语言标签、完整 import 和后续目录均正常，且代码块逐字对比通过。没有剩余阻塞项。

### keep

保留两条清楚的 SDK 接入路线、可复制且明确标注业务边界的示例，以及不打断阅读的文档助手。

## 限制

- 本地启动器没有公网发布，生产身份、持久限流、日级额度及运维仍由正式宿主负责。
- 文档检索按关键词和中文双字片段排序，无外部向量数据库；引用来自可见资料，但不自动证明答案语义正确。
- 当前 IM SDK 使用安全 Markdown，来源来自公开白名单；公开思考仅显示供应商实际返回并获准保留的内容，不补造推理。
- 接入教程需随公开 API 变更维护；六份主要示例共用 TypeScript 源文件，接口参考和示例配置两份手册直接同步。宿主鉴权和业务实现由接入方验收。
- 最新能力接入回归包含既有 PostgreSQL 与故障矩阵，详见 DOCS-51 至 DOCS-57；本地实验和合成截图不能替代真实供应商、生产部署或真机验收。
