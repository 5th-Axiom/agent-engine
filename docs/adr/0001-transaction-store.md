# ADR 0001：事务记录与内部边界

技术方案 §5.4 明确逻辑记录不要求同等数量物理表。首版采用按 Session 分区的事务聚合记录保存检查点与历史，业务操作去重账本独立于 Session 生命周期；事件仍使用有序独立记录，管理锁使用专用 PostgreSQL 连接。事务不得覆盖模型或工具网络调用。

这样可以先证明单进程恢复契约，避免跨表拼接状态的中间态。不宣称高吞吐或多 Worker。所有内部状态变更仍需 CAS，并在同一事务追加语义事件。后续拆分物理表必须保持事务 SPI 及其契约测试。
