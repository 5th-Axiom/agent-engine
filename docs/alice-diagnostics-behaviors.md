# Alice 诊断接入行为卡

| 卡 | 合约与实现证据 | 实测与边界 |
| --- | --- | --- |
| DIAG-01 | Chat contextRef + resolveRunContext；宿主 PostgreSQL admissions 首次解析胜出 | 两个通用新测试及 Alice 并发/重试/重启测试；浏览器任意 context 不受信 |
| DIAG-02 | 独立 Biz 身份验证、active superadmin 与精确授予来源/用户 | 真实 PG 合成管理员、无 Cookie/跨站/禁用/撤权；线上现有管理员有效身份租约、状态及 62 个授权用户计数实测；用户截图确认问答入口，已知故障对照待完成 |
| DIAG-03 | libpg_query AST + 每关系字段/ownership/时间投影 + PG 只读角色 | 真实 PG 联表/聚合/CTE、危险语句拒绝、字节/行/次数上限、独立连接取消；未接入任意 shell 或 URL |
| DIAG-04 | 消息/运行/阶段、任务/事件、模型 attempts 与固定版本资料 | 扩展消息/投递/子任务/探索/历史配置；真实模型三类合成故障对照通过；线上现存记录链路与证据通过，真实故障根因仍区分推断 |
| DIAG-05 | 独立证据 API、签名游标、当前授权、到期正文清除 | 跨身份/查询/规则撤销/到期拒绝，浏览器分页失败重试；历史快照不宣称实时数据 |
| DIAG-06 | 单 Engine writer、SIGKILL 恢复、固定双仓与本地包、独立发布/回滚 | 真实 PG 强杀恢复、来源拒绝及正式部署通过；真实往返回滚和服务器隔离库备份恢复通过，业务服务未重启；周期备份与隔离库恢复实测通过；异地适配已实现，目标仍待用户提供 |
| DIAG-07 | 用户实体快照与事件窗口区分；真实计数产生独立证据 | 线上授权范围当前 62、窗口新增 0；老用户 / 未授权用户 / 联表窗口 / 撤权测试；真实模型合成原句 1 次查询 |
| DIAG-08 | 目录时间单位与实时列权限复检；docs/code/logs 覆盖分别报告 | PG 撤权后隐藏缓存字段，保留其他关系；资料空搜索、版本匹配与重复 ID 拒绝；线上 3 份文档 / 7 份部署源码与脱敏日志已接入；范围、截断、过期分别声明 |
| DIAG-09 | 新旧镜像先检查配置；旧 writer 停止后变更数据库；回滚失败恢复当前镜像；Compose 不读取 SSH 脚本 stdin | 隔离命令驱动验证配置拒绝、健康失败、stdin 消费及成功；真实 e29 配置拒绝不影响服务；a6 → cfc → a6 往返回滚健康且原证据可读 |
| DIAG-10 | 16 个获准关系；工具确认/租约、子任务、会话/投递、探索阶段/outbox；RLS 专用 SELECT 策略 | 原 RLS/FORCE RLS 保留；当前管理员过滤、子表所有权和隐藏字段隔离通过；线上四类对象链路与保留证据可读；真实 Provider/线上数据核验人数、调用配置、探索状态通过（1/1/7 次查询） |
| DIAG-11 | 模型调用 JSON 固定白名单投影；Prompt key/version/source_hash、数值/布尔参数、错误码 | SQL 无法读取原 JSON 或隐藏正文；合成真实模型识别 v7 / temperature 0.4；历史缺失值保持未知 |
| DIAG-12 | 固定容器宿主采集，logs.search 用户/Trace/时间/服务隔离、同一查询预算、独立证据 | 脱敏/过期/版本错配/取消/撤权通过；线上 24h 查询达到 100 行边界时明确 truncated，未把采集丢弃视为无异常 |
| DIAG-13 | 从实际完整部署 commit 生成资料清单，SHA-256 校验；在线版本观察新鲜度门禁 | 业务仍为 1f36c372；10 份文件校验通过；版本变化或观察过期时拒绝旧源码；不读取工作区 HEAD |
| DIAG-14 | 每日 diagnostics-only 备份、唯一临时库恢复、14 份本机保留；可配置加密异地适配 | 实际 systemd 运行与 pg_restore/schema/记录复核通过，业务未重启；异地未配置时不发出外部请求且回执明确 not_configured |
| DIAG-15 | 独立页面入口使用 SDK 语义色、44px 蓝色按钮、外链图标和键盘焦点，保留新标签页链接 | 1440/390px 合成浏览器检查：不溢出、对比度 7.21:1、Tab/Enter 打开独立页、noopener、0 次查询及 0 页面错误；构建通过。2026-09-15 Alice df7109e5，线上发布结果见实施计划 |
| DIAG-16 | 宿主可显式关闭 Loop 累计上限；省略仍继承默认，有限 Policy 不允许 null 绕过 | SDK 合成运行完成 67 步/68 请求/66 能力调用、142,800 Token 和超过 120 秒的模拟活动时间；取消、有限 Override/Token Policy、PostgreSQL 故障恢复通过。Alice 原 49 项回归通过；用户后续将运行时间改为 5 分钟，88fac1bc 已部署，1 项定向 HTTP/PG 与线上新会话实际配置确认 timeoutMs=300000、其余三个计数 null/无 Token 预算。旧会话仍保留冻结配置；真实模型既有验证与维护缺口见实施计划 |

| DIAG-17 | 模型和聚合工具使用本轮剩余 5 分钟；累计查询默认不限且保留审计；Run 截止与模型自身超时区分 | SDK 253 项通过；慢流、SQL/日志回归与线上结果见实施计划。模型容量、SQL 资源和历史范围独立边界明确记录 |

实现位于 Alice 仓库 diagnostics/，业务语义不进入 SDK。详细命令、数量、真实模型用量及待办以 Alice diagnostics/ACCEPTANCE.md 为准。没有把测试跳过或空接口计为完成。
