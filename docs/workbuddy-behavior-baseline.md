# 行为基线与验收卡片

参考版本：WorkBuddy AI 5.4.2 / CodeBuddy CLI 2.132.0。所有 Fixture 独立编写。未执行 WorkBuddy 双端实测，不把源码机制当作可靠性保证。下表路径均相对只读参考仓库 `/Users/circle/git/workbuddy-reconstructed`。

| ID               | 参考证据、等级                                                                                                                | 采用决策及新契约                                            | 确定性验收设计                                                                 | 当前状态                                                                                                                                                                                                                                                                           |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WB-REPLAY-01     | `recovered-source/packages/workbuddy-server/src/backend/prompt-replay-safety.ts.recovered.js:15`，已读源码；unsafe 标记优先   | 改造：写操作未知必须核验；业务 namespace 不含实现版本       | 写成功后回包丢失、换 Call ID/版本/Session；派发计数仍为 1；取消后核验再终结    | 新实现已验：ledger、process-crash、checkpoints、cancellation；真实 PG/HTTP/SIGKILL、跨 Session/版本同键、核验证据和取消均通过。WorkBuddy 未双端实测。                                                                                                                              |
| WB-EVENT-01      | `recovered-source/packages/workbuddy-server/src/session/session-manager.ts.recovered.js:3920`，已读源码；hydration 缓冲及排空 | 改造：事务分配序号、固定高水位、有界消费者                  | 同时补拉和提交事件，迟到 Usage；断线重连 ID 不变；慢消费者明确关闭             | 新实现已验：subscriptions、postgres；固定高水位、事务顺序、1010 个 delta 后完整快照、恢复撤销、慢消费者及缓存页中途撤权通过。                                                                                                                                                      |
| WB-COMPACT-01    | `recovered-cli/codebuddy-headless.beautified.js:181333`，已读源码；重入、pending interruption、空历史及递归检查               | 改造：完整消息组、有界分块、失败保留检查点、统一用量        | 超大单组拒绝；摘要超窗分块；暂停组不拆；摘要失败不改历史                       | 新实现已验：capabilities、permissions、retention；完整组摘要、有界合并、旧结果裁剪、统一用量、原始历史分离和派生来源检查。估算及保留粒度见 ADR 0002/0003。                                                                                                                         |
| WB-SESSION-01    | 使用说明 §3/§14；源码分支待核对                                                                                               | 改造：无目录依赖；创建/受理幂等；配置 CAS；Run 推进锁       | 回包丢失重投、版本更新与受理竞态、重启恢复及关闭句柄                           | 新实现已验：engine、options、admission、interactions、checkpoints；创建/受理幂等、配置 CAS 与受理竞争、冻结来源、回包丢失、交互重启和关闭句柄。                                                                                                                                    |
| WB-MODEL-01      | 使用说明 §10/§11；`recovered-cli/codebuddy-headless.beautified.js:170180` 原生签名分支已读                                    | 改造：Thinking 续接加密、草稿撤销、冻结 Step 重试           | 半流工具不执行；Thinking 不降级；拒答/截断不修复；续接原样保留                 | 本地协议已验：protocols、options、fallback、checkpoints；两种真实 HTTP SSE、原生签名/加密续接、错误终止门、冻结重试及小窗口 Fallback。2026-09-11 授权 DeepSeek 两种兼容协议真实 smoke 通过，含 Thinking 原生保护/工具往返/多轮续接；WorkBuddy 未双端实测。                         |
| WB-CAPABILITY-01 | 使用说明 §5–§8；源码分支待核对                                                                                                | 改造：显式能力、控制混批拒绝、KB sourceId、Memory CAS       | 未声明能力不可见；审批不可扩权；Skill 混批零执行；伪造引用失败；跨 Session CAS | 新实现已验：capabilities、capability-boundaries、permissions、remote-tools、interactions；显式能力、Skill 混批/加载失败/恢复、KB 来源、Memory CAS/逐条候选/读取重试、远程契约漂移。                                                                                                |
| WB-USAGE-01      | 使用说明 §12；`usage-effect-projector` 的快照投影源码已读                                                                     | 新增 Attempt 账本：snapshot 替换、delta 去重、未知/币种分开 | 重复/迟到报告不相加；预算提交前检查；终态后超额只修订 Usage                    | 新实现已验：usage、cancellation、telemetry、integration/retention；快照/增量、未知不补零、多币种、提交前及迟到预算、删除后修订、重启后单次收尾及净差值指标。2026-09-11 授权 DeepSeek 的普通、Tool、Thinking Run Usage 完整性实测通过；供应商迟到修订等异常仍由确定性契约测试验证。 |
| AE-CONFIG-01     | 新框架新增，技术方案 §2.5/§5.2                                                                                                | 严格 JSON 配置，无隐式能力，白名单合并，硬上限拒绝          | 未知字段、函数/变换、重复名、非法引用失败；默认与显式来源分离                  | 新实现已验：foundations、options、admission；严格输入、Schema 转换/错误、显式能力、有效来源/版本、覆盖收紧、并发配置 CAS。                                                                                                                                                         |
| AE-STORE-01      | 新框架新增，§5.4                                                                                                              | 同库事务、管理锁、检查点 CAS、失锁停止派发                  | 真实 PostgreSQL 并发/回滚/失锁/崩溃验证，非内存替代                            | 新实现已验：postgres、ledger、checkpoints、process-crash、interactions；事务回滚、管理锁/失锁、原子认领、各关键检查点恢复及真正 SIGKILL。                                                                                                                                          |
| AE-SECURITY-01   | 新框架新增，§5.18                                                                                                             | 宿主可信身份，目标/Secret/Binding/资料逐次授权，同源清除    | 跨租户查询/引用/描述/订阅拒绝；SSRF/重定向阻止；Debug 不泄露载荷               | 新实现已验：security、permissions、options、retention、capability-boundaries、debug；可信主体/引用/目标约束、SSRF/重定向、逐条撤权、保留/删除派生清理和迟到回写保护。边界见 ADR 0003。                                                                                             |
| AE-DEBUG-01      | 使用说明 §13；无统一控制台实测证据                                                                                            | 新增只读 Console 和独立健康遥测；无真实重放/调度            | 查询关联 Attempt，写方法拒绝；DB 断线仍报告健康                                | 新实现已验：debug/telemetry 契约、verify-debug 桌面/移动截图、Impeccable 独立审查、verify-monitoring 真实 HTTP 指标及脱敏 Trace/日志接收。外部告警投递与真实 Replay 不在首版。                                                                                                     |

每个卡片只有在对应测试实际执行后才更新通过范围。M0–M3 完成状态以 implementation-plan.md 为准。

## 追加：本地手动测试宿主

| 卡片             | 参考与边界                                                                                              | 新实现验证                                                                                                                                                                                                                                                |
| ---------------- | ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AE-PLAYGROUND-01 | 用户明确追加的本地测试页；非 WorkBuddy 已验证行为。只调用已授权 SDK 能力，Debug 保持只读；见 ADR 0004。 | playground HTTP 契约、verify-playground 浏览器交互、桌面/移动截图及独立审查已通过。真实浏览器普通问答、Thinking 工具调用/Usage 和 Debug 导航通过；真实 PostgreSQL 服务重启后的 Thinking 续接通过；按持久受理序号排列，逆序 Store 和旧记录兼容性另有回归。 |

## 追加：通用聊天接入

| 卡片           | 参考与边界                                                                                                                              | 新实现验证                                                                                                                                                                                                                                                                                                                      |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AE-CHAT-SDK-01 | 用户明确追加前端 SDK；Alice 仅参考 Token/原子/业务/页面分层，非 WorkBuddy 双端实测行为。Node 桥使用宿主可信身份；不复制私有组件或会话。 | chat-core 契约与 chat-server HTTP 测试通过；18 个 Chromium 检查覆盖悬浮/嵌入、换肤、输入、取消、工具/用量和卸载；丢回包重试由核心契约验证。六视图独立审查 ship。真实 DeepSeek 普通/Thinking 工具、Debug、PostgreSQL 重启后的同会话续聊通过；本地安装包在独立项目安装、类型和浏览器打包通过。范围见 frontend-sdk-acceptance.md。 |


## 合并审查新增边界证据（2026-09-11）

- AE-SECURITY-01 / WB-COMPACT-01：模型完成前撤权/到期拒绝；结果与受控查询一致；历史操作回执复用必须通过本次权限与审批，原始来源与当前操作资料授权也须有效。见 `review-regressions`。
- AE-CONFIG-01 / WB-USAGE-01：预算缺省继承硬上限、不同币种拒绝、同 `$id` 隔离编译、Skill 工具子集一致性、部分缓存 Usage 保留未知。见 `review-regressions` / `review-protocol`。
- WB-MODEL-01：持续有效 Thinking 流不误超时，纯 ping 超时；超限批次与未知工具按现有业务约定进入有限修复；无备用目标仍遵守 retryOn。密钥等待与退避有界。见 `review-protocol` / `review-regressions`。
- WB-SESSION-01：501 个公开创建的会话可按稳定顺序分页到达；聊天桥跨页筛选助手空间。
- 此次为新引擎回归验证，不代表已完成 WorkBuddy 双端实测；逐项证据与真实 Smoke 状态见 [审查修复记录](review-remediation.md)。
