# Agent Engine 实施清单

状态：M0–M3 首版范围已实现并通过验收，真实 Provider smoke 已通过。证据见 [实施记录](implementation-status.md) 和 [行为卡片](workbuddy-behavior-baseline.md)。

权威契约：[technical-design.md](technical-design.md)。项目路径 `/Users/circle/git/agent-engine`；参考项目 `/Users/circle/git/workbuddy-reconstructed`，仅只读使用。

## 执行方式

逐模块记录参考证据、目标断言、实现与实际执行结果。接口存在、Mock、失败或跳过项不计为真实集成验证。按 M0–M3 连续实施，不在 Demo 阶段停止；M4/M5 不扩入本轮。

## M0：契约和基础设施准备

- [x] 完整阅读设计、参考说明与两轮可靠性审查，核对相关恢复源码。
- [x] 建立行为卡片、证据等级与新框架差异表。
- [x] TypeScript workspace，SDK / Debug / Testing 三包及受测依赖锁文件。
- [x] Config、公共类型、事件 Schema、错误 DTO、状态机、默认值/来源/版本。
- [x] Scripted Model、Fake Capability、Clock、故障注入设施。

## M1：可靠的最小执行闭环

- [x] Session/Run 创建与受理幂等、配置预览/冻结/CAS、取消、归档和删除。
- [x] PostgreSQL 事务 Store、显式迁移、管理锁、Run 锁、检查点版本与失锁保护。
- [x] 标准模型协议、流式草稿提交/撤销、Thinking 原生载荷保护（本地协议验证）。
- [x] Binding Tool、输入/输出校验、执行证据和业务幂等 Ledger。
- [x] Retry/Repair、未知写入保护、模型/能力/宿主控制调用额度与信号量。
- [x] Event、Attempt、Usage 从真实执行闭环进入同库持久记录。

## M2：完整能力与恢复

- [x] 崩溃恢复、Pending Input、问题/表单、审批、答复幂等、Operation 证据核验与独立维护额度。
- [x] 高水位补拉/订阅、背压、过期游标、完整快照、逐条撤权和同源清理。
- [x] Tool / Skill / Knowledge / Memory 共享管线、版本依赖、Scope 及候选授权。
- [x] HTTP 与远程 MCP；描述/执行隔离、Schema 漂移拒绝、实际幂等传递。
- [x] Skill 控制边界/恢复、KB sourceId、Memory CAS/写入核验。
- [x] 冻结请求、工程裁剪、有界摘要分块/合并、全链路预算和迟到 Usage。
- [x] 关键故障、并发、跨主体、取消、未知副作用、失锁及 SIGKILL 矩阵。

## M3：首版交付与验收

- [x] 第二种模型协议、组合能力校验、Thinking 续接和受限 Fallback（本地真实 HTTP SSE）。
- [x] 只读 Debug：Config、Run、Step、Attempt、Operation、Usage、错误，鉴权及桌面/移动验证。
- [x] Telemetry：关联 Trace、净修订指标、脱敏日志、独立健康路径及真实本地 HTTP 接入示例。
- [x] 六个公开可运行场景，含基础、Tool、Skill、知识库、Memory、监控、Usage 和覆盖配置。
- [x] 最终类型/三包构建/完整测试通过；Node 26.3 与 Node 22.23.2 各 100 项、0 跳过。
- [x] 授权真实 Provider 的基本响应、Tool、Thinking/Usage smoke。alice 用户密钥驱动 DeepSeek 的 OpenAI-compatible 与 Anthropic-compatible 两条路径，Thinking 原生保护及续接实测通过。
- [x] 依赖/Secret/隔离检查与首版验收报告收齐；凭据集中管理、配置引用，首版总验收关闭。

## 追加交付：本地交互测试页

用户明确追加了可手动发问的本地页面，属于独立开发宿主示例，Debug 保持只读；边界见 ADR 0004。

- [x] `pnpm playground` 同时启动测试页与 Debug，并使用独立 PostgreSQL 开发库。
- [x] 已授权模型选择、自由对话/合成只读工具、文本草稿、取消、继续、历史记录、用量和会话 Debug 导航。
- [x] HTTP 访问边界、输入拒绝、幂等、取消、原生数据隔离测试通过。
- [x] 桌面/移动浏览器交互通过，独立页面审查为 `ship`，无要求修正项。
- [x] 真实浏览器下普通模型问答、Thinking 工具查询及 Debug 往返通过。
- [x] 服务重启后的真实 Thinking 会话续接与最终操作文档归档。
- [x] PostgreSQL 非时间序返回的实测问题已修正：Run 持久受理序号、旧记录事件恢复及逆序 Store 回归。

## 追加交付：新手使用文档

追加的使用文档已按新手路径整理：README 与文档导航、从零启动、页面与 Debug 操作、集中配置、错误排查、SDK 入门和接口参考。两个完整 TypeScript 入门示例通过类型检查并实际运行；配置初始化在独立临时目录验证重复执行、保留原 Key、权限和加载器兼容；现有 PostgreSQL basic 示例再次通过。此次仅更新文档，未将历史全量测试或真实 Provider 结果记为本轮重测。

## 追加实施：通用前端聊天 SDK

- [x] 公开协议、HTTP transport 和状态控制器，输入去重、取消与卸载边界；丢回包与陈旧响应契约通过。
- [x] Token / 原子组件 / 业务组件 / 页面分层及公开导出；独立项目的安装、声明、SSR 导入及浏览器打包通过。
- [x] 悬浮 IM 入口、嵌入式页面、主题/皮肤/品牌色、PC 与移动端；18 个 Chromium 功能检查通过。
- [x] 受鉴权的 Node 接入桥，助手白名单、跨主体隔离与脱敏；真实 HTTP 集成测试通过。
- [x] 后台接入示例、组件展示、新手接入文档和按实装生成的设计规范已交付。
- [x] 27 文件 / 110 测试通过，真实模型、Thinking 工具/Debug、PostgreSQL 重启续聊通过；六视图独立审查为 ship。

具体边界见 [ADR 0005](adr/0005-embeddable-chat-sdk.md)，实际结果见[前端 SDK 验收记录](frontend-sdk-acceptance.md)。不以接口或静态演示作为完成证据。

## 首版完成判定

所有必需契约有实现及验收证据后才能声明首版完成。真实 Provider smoke 未通过时不得关闭首版总验收。源码已确认的 WorkBuddy 行为与其实际运行验证分别标记；没有对照环境不宣称实测对齐。

M4/M5（独立部署、多 Worker、扩容、真实模型或工具重放）明确延期。未发布 npm 包、未部署服务、未执行生产业务操作。

## 验证记录

- 2026-09-11 15:27–15:34：聊天 SDK 六包构建、`pnpm check`（Node 26.3）和 Node 22.23.2 全量各 27 文件 / 110 项通过；18 个 SDK 浏览器检查和旧测试页回归通过，浏览器错误 0；独立项目 tarball 安装/导出/声明/浏览器构建通过。真实普通/Thinking 工具问答、Debug 与 PostgreSQL 重启后的同会话续聊通过；独立 UI 审查 ship，设计文档完成。
- 2026-09-11 14:21–14:22：加入运行受理顺序及旧记录恢复回归后，`pnpm check` 与 Node 22.23.2 完整测试各 25 文件 / 100 项通过、0 跳过；`pnpm verify:playground --functional-only` 再次通过，浏览器错误为 0。
- 2026-09-11 14:13：新增页面后的 `pnpm check` 与 Node 22.23.2 完整测试各 25 文件 / 99 项通过、0 跳过；历史首版记录保留如下。

- 2026-09-11 13:04–13:05：`pnpm check`（Node 26.3），24 文件 / 94 测试通过，0 跳过；随后 Node 22.23.2 相同矩阵全部通过。
- 新增四项本地凭据契约：引用隔离、缺失/占位拒绝、文件权限、错误脱敏。
- 六个公开示例再次通过；生产依赖官方 Registry 审计零已知漏洞，Git 可纳入文件的真实凭据匹配为零。
- `pnpm verify:monitoring`：真实 HTTP 抓取指标，收到 6 个脱敏 Trace、1 个失败信号。
- `pnpm verify:debug`：桌面/移动无页面溢出，独立 UI 审查和交接已完成。
- 2026-09-11 13:03–13:04：alice 授权密钥下，两种真实兼容协议 smoke 通过，覆盖基本响应、只读 Tool、Thinking 原生保护/工具往返/多轮续接及 Usage；旧鉴权阻塞解除。

[首版验收报告](release-acceptance.md) 列出完整证据与首版边界。

## 官方文档站（用户追加范围）

- 独立 `examples/docs-site` 宿主，20 篇按前端 / 后端 SDK 接入组织的使用手册与参考，默认接入现有 IM SDK；不修改核心引擎或扩展 M4/M5。
- 搜索、文章导航、代码复制、上下文草稿、主题、手机全屏 IM、匿名主体隔离与仅阅读模式的确定性浏览器/HTTP 检查已通过；`pnpm typecheck` 通过。
- 真实模型的文档检索、普通聊天、范围问答及刷新恢复三轮历史通过；修复真实检索片段过长导致的上下文超限，增加字节上限回归。六包构建、冻结锁文件检查通过，未改核心包、未部署或发布。
- 桌面/移动视图及真实聊天已复核，设计规范已记录。此侧对话禁止子代理，采用本线程降级终审，结论 ship；具体行为卡、证据范围及可复现命令见 [文档站验收记录](../examples/docs-site/ACCEPTANCE.md)。

- 按用户最新反馈完成接入方向修正：前端安装、挂载、登录、主题和生命周期；后端初始化、模型、会话、工具、知识库、事件和 Debug；本地演示只保留为附录。六份主要代码示例直接读取受类型检查的源文件，代码块逐字比对和确定性工具 / 续聊通过。
- 新版真实助手的前端接入、后端续聊和普通聊天三轮问答通过；原样后端示例在独立临时 PostgreSQL 中完成真实模型两轮对话。补充已有 SDK 对精确模型 origin 的可选网络配置，验证后只清理本次创建的临时库。未改写凭据文件。

## 2026-09-11 文档助手双模式（追加范围）

- [x] 默认 `/ai/` 使用现有 `mountChatPage`；传统模式继续保留文章、搜索、目录、复制与悬浮 IM。
- [x] 共享历史与同标签页草稿；旧文档会话在下一次发送时升级只读工具配置，保留原 ID 与历史。
- [x] 六个只读 Binding：docs.search、docs.read、api.lookup、examples.find、code.search、code.read。公开源码来自启动时 Git HEAD 白名单快照；文档正文按启动时内容摘要标记版本。文件位置与来源可打开核对。
- [x] 确定性 Engine 两条查询链、原文分页还原、临时 Git 仓库隔离与路径边界、26 组文档站验证通过；类型检查与 19 组聊天 SDK 浏览器回归通过。
- [x] 真实默认模型 + 独立 PostgreSQL 四轮接入／普通聊天／后端续聊／API 与源码问答，刷新恢复通过。实际检索工具与有效源码出处均有证据；不保证模型每句话正确或严格遵守引用格式。
- [x] 独立 UI 复核对欢迎区密度、设计文档同步两项修复确认，disposition: ship（仅覆盖所列修复）；PRODUCT、DESIGN 与 sidecar 已同步。凭据精确匹配扫描为 0。

用户界面追加范围仅修复 SDK 空态滚动、推荐问题换行和重复留白；未扩展 M4/M5。详细行为卡和边界见 [文档站验收记录](../examples/docs-site/ACCEPTANCE.md)。

## 2026-09-11 左侧会话与工具面板（追加范围）

- [x] 通用 chat-ui 页面与浮窗共享左侧会话列表、当前会话信息和工具面板；窄容器可展开侧栏，键盘进入内容、滚动和 Escape 焦点返回已验证。
- [x] chat-server 从保存会话及活动运行的配置分别投影工具摘要，支持显式公开 toolDisplay 文案；身份与权限校验保留，不暴露原始说明、参数 Schema 或执行器。
- [x] 2 文件 / 14 项后端与控制器测试，28 组文档站检查，19 组 SDK 浏览器回归、类型检查与构建通过。接入文档同步新增入口、宽度规则、可选协议字段及 toolDisplay 用法。
- [x] 独立 UI 复核将唯一键盘滚动入口修复评为 resolved，disposition: ship（所列修复范围）。
- [x] 重启 4320 文档站后，真实本地服务／PostgreSQL 的六项面板 smoke 通过，不调用模型。
- 最新真实模型四轮均运行完成，但后端接入回答漏掉 createSession，完整内容检查未通过；保留这一准确性限制，未通过重试挑选成功结果。

没有扩展 M4/M5；本次行为与验证边界见 [DOCS-19 至 DOCS-22](../examples/docs-site/ACCEPTANCE.md)。

## 2026-09-11 传统模式介绍页（后续追加）

- [x] 产品介绍成为传统模式首篇：一句话用途、四项核心特点、前端／后端 SDK 两列使用表；右侧入口进入已有完整接入示例。
- [x] 同步导航、搜索内容、AI 返回入口、PRODUCT 与设计规范；类型检查、构建和 28 组文档验证通过，两条产品路径与五张桌面／移动截图已核对。
- [x] 未扩展产品能力、发布或部署；真实模型准确性限制保留原验收记录，本次只验证介绍与阅读路径。

## 2026-09-11 合并审查修复记录

CodeRabbit 与 Codex 合并后的 31 项确认问题已逐项修复，两个条件性界面问题同步加固。范围、测试与未执行验证的区分见 [审查修复记录](review-remediation.md)。新增公开 API 行为覆盖结果撤权/过期、缺省预算硬上限、旧回执复用授权、Schema 重用、工具子集、分页和超时；标准协议补充 Thinking 活动、心跳、未知工具、超限批次及部分 Usage 验证。Node 26.3.0 / 22.23.2 各 29 文件、134 项通过，使用并清理独立 PostgreSQL 测试库；两种真实兼容协议 Smoke 通过，CodeRabbit 增量复查的 3 条 minor 意见已处理；未扩入 M4/M5。


## 2026-09-11 双模式体验优化（追加范围）

- [x] SDK 合并标题操作、左栏新对话与紧凑输入区；文档站 440px 停靠助手、16px 正文和更轻的分隔线。
- [x] 文档与 AI 间的文章/草稿/阅读位置衔接，按回答展示白名单引用；前端教程框架标签与分类目录。
- [x] 类型检查、构建、32 组文档站检查、20 组 SDK 浏览器检查和 7 项 chat-core 契约测试通过；明暗主题/五种宽度/工具键盘路径验证通过。重启 4320 后以独立浏览器上下文确认桌面与移动、正文与助手不重叠，未调用真实模型。详见 DOCS-24 至 DOCS-28。

未扩展 M4/M5，不改变引擎工具权限或调用模型的行为；消息正文仍为纯文本，不宣称 Markdown 渲染已交付。

## 2026-09-11 场景接入与图片能力优化（用户追加）

用户允许为改善接入直接调整公开 API；产品交付形式仍待确定。本轮扩展图文消息，不扩入 M4/M5。

- [x] 25 篇场景文章、独立能力教程、完整后端/前端/全栈示例及带相对本地依赖包的导出；三条路径在临时目录安装并运行，包含后端跨进程续聊、五个能力场景和前端跨域认证。
- [x] 类型化作者输入、defineBoundTool、RunInput.skill、可定位配置错误与公开符号/默认值/授权参考；保留旧文本调用和手工 Binding。
- [x] 加密附件、两类视觉协议、历史工具图片、冻结重试、PostgreSQL 崩溃恢复、撤权/到期/损坏/存储版本和上传中断清理；浏览器选择/粘贴/拖拽/预览/移除/重试。
- [x] 搜索命中片段和章节锚点、文件名/语法高亮/复制、聊天安全 Markdown；受检场景代码与页面一致。
- [x] 2026-09-12：pnpm check 31 文件/146 项通过；25 篇/32 组文档与20组聊天浏览器回归、图片专项和三路径独立安装通过。真实视觉三阶段（多图随机编号、重启颜色追问、工具历史图片）通过；Anthropic 视觉仅本地 HTTP 协议验证，未宣称真实供应商实测。
- [x] [优化总结](usage-optimization-summary.md)、[ADR 0006](adr/0006-scenario-usage-and-images.md) 与 DOCS-29 至 DOCS-34 记录当前契约和限制；独立界面复核 ship，设计文档和 sidecar 同步。

正式产品交付形式继续待定，未提交、发布或部署。4320 本地文档站已以可用视觉 Profile 重启，实际入口和能力投影检查通过；源码检索仍只索引已提交白名单，未提交 API 变更可从更新后的场景文章及 API 参考读取。

## 2026-09-12 聊天处理过程（用户追加）

用户要求先评估架构，支持则直接实现。结论：现有持久事件、授权回读和 UI 分层支持；主要补聊天投影和过程组件，无需改造调度/恢复模型。

- [x] Engine 补 model.streaming 的真实 phase、Skill 开始事件和 inspectSession.observedAt；不记录私有思考正文，不改变模型重试、写结果未知和预算契约。
- [x] chat-server 有界过程投影、逐次数据授权、刷新回读、显式安全查询/结果摘要；ChatRun.process、usage.costs、activity.kind 均为兼容新增字段。
- [x] 共享 createRunProcess：执行时展开、完成后可收起、保留主动查看状态；模型/工具/Skill/知识/记忆/重试/等待与真实耗时用量。修复浅色品牌色的状态对比度和详情更新时的键盘焦点/滚动连续性。
- [x] pnpm check 32 文件/150 项通过，含真实 PostgreSQL、0 跳过；新过程专项 HTTP/浏览器、25 篇/32 组文档、20 组聊天及图片回归通过。最后小幅投影加固后的类型检查与过程/chat-server 专项也通过。
- [x] 4320 文档站重启后，已授权 DeepSeek Profile + 真实 PostgreSQL smoke：两次模型请求、一次文档检索，model/thinking/tool 步骤、已知用量及刷新恢复通过，浏览器错误 0。首次 smoke 的浏览器断言失败，数据库确认该轮已完成；脚本改为显式等待读取最新快照后复验通过，不把异步监听中的旧快照当作完成状态。
- [x] 独立界面终审确认对比度与阅读连续性两项修复 resolved，disposition: ship，结论仅覆盖所列修复。架构评估、API 和限制记录在 [聊天过程架构与实现](chat-process-architecture.md)，行为卡见 DOCS-35 至 DOCS-38。

本次没有为文档助手开启跨会话记忆或新增审批表单，仍通过宿主已有配置/接口接入；未扩入 M4/M5，未提交、发布或部署。

## 2026-09-12 输入编辑与模型选择（用户追加）

- [x] 复用 composer / controller / chat-server / Engine 分层，接入既有每轮模型覆盖和显式 Skill；无需修改引擎执行架构。
- [x] 原生编辑与撤销/重做、自动增高/展开/选区、IME 保护、发送快捷键和上一条文字找回；图片草稿保留。ASR 未接入，麦克风禁用并解释。
- [x] 安全模型/Skill 目录、下一轮选择、冻结重试、刷新恢复与配置撤销检查；docs 空闲旧会话通过授权 GET/CAS 更新目录，执行中的 Run 保持冻结。
- [x] pnpm check 32 文件 / 153 项、0 跳过，含真实 PostgreSQL；输入编辑专项、25 篇 / 33 组文档、20 组聊天、图片及处理过程浏览器回归通过。独立包声明/Node/浏览器消费通过，三条导出示例更新。
- [x] 4320 实际入口切换至已授权 DeepSeek 普通模型并发送，1 次请求 / 1527 Token，刷新恢复、浏览器错误 0；未测试所有 Profile 或原生移动键盘。
- [x] 独立终审对小屏选择器截断 F1 判定 resolved、ship（仅该修复范围）；[输入编辑总结](composer-alignment.md)、场景文档、API、README 与 DOCS-39 至 DOCS-42 同步。

跨模型续聊遵守原生历史和图片能力约束；不通过丢弃受保护历史放宽契约。语音、任意文件/工作区引用未实现，未扩入 M4/M5；未提交或发布。


## 2026-09-12 回复内容与动态展示（用户追加）

- [x] 只读分析 WorkBuddy reasoning/text buffer/agent/status 的职责；独立补齐适配器 → Engine 事件 → 公开投影 → UI，执行架构无需重构。
- [x] 新增显式 expose: content 与 host thinkingDisplay，保留 summary 语义及 none 默认；权限、保留策略、原生签名隔离与失败草稿清理均有契约。技术设计 5.11 记录新增原因与限制。
- [x] 思考／阶段文字／工具／答案分区，完成基础阶段下沉详情；字素缓冲、稳定 Markdown DOM、焦点/复制、用户阅读位置、历史与减少动态效果。独立界面复核 ship，仅五张回复截图与抽查代码范围。
- [x] pnpm check 32 文件 / 157 项、0 跳过；25 篇 / 33 组文档、20 组聊天、图片/过程/输入/回复专项；独立包和三路导出消费者均通过。初次新测试发现事件 schema 会丢弃 format，已同步 schema 并复验；旧聊天脚本按新的完成层次调整断言后通过。
- [x] 4320 + PostgreSQL + 已授权默认视觉 Profile 的真实 smoke：3 次模型请求、3 次工具调用、3 段实际公开思考、10886 Token；运行中有正文、刷新后仍可展开，错误 0。deepseek-thinking Profile 的首次请求返回 MODEL_PROVIDER_ERROR，未宣称该 Profile 可用。
- [x] [改动与配置总结](reply-display-alignment.md)、前端 SDK、公开 API、场景文章及 DOCS-43 至 DOCS-46 同步。保留原有未提交工作；未提交、发布或部署。

公开过程保持原有大小与回读上限；旧未保留思考无法补回。当前传输仍为快照轮询，本轮未新增语音、公式、语法高亮、M4/M5 能力。
